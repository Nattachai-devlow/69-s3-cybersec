services:
  db:
    image: postgres:16-alpine
    container_name: 69-s3-db
    ports:
      - 54399:5432
    environment:
      - POSTGRES_USER=nattachai
      - POSTGRES_PASSWORD=JuniorG4me@PRM
      - POSTGRES_DB=nattachai
  pgadmin:
    image: dpage/pgadmin4:9.16
    container_name: 69-s3-admin
    ports:
      - 8080:80
    environment:
      - PGADMIN_DEFUALT_EMAIL=nattachai@pgadmin4.com # อีเมลสำหรับใช้ Login เข้า pgAdmin
      - PGADMIN_DEFUAT_PASSWORD=JuniorG4me@PRM  # รหัสผ่านสำหรับใช้ Login เข้า pgAdmin
    depends_on: #
      - db 